import { Box } from "@chakra-ui/react";
import { LayoutBottomBar } from "@src/components/organisms/layout";
import React from "react";

export const HomeButtonBar: React.FC = (): JSX.Element => {
  return <Box as={LayoutBottomBar}>1</Box>;
};
